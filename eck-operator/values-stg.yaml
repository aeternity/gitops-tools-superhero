kibanaIngress:
  enabled: true
  hosts:
    - host: "kibana.stg.superk8s.de"
      paths:
        - path: "/"
  tls:
    - hosts:
      - "kibana.stg.superk8s.de"
  svcName: "kibana-kb-http"
  svcPort: "5601"
