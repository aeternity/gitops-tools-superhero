---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: hcloud-cloud-controller-manager-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hcloud-cloud-controller-manager.labels" . | nindent 4 }}
spec:
  encryptedData:
    {{- range $key, $value := .Values.encValues }}
    {{ $key }}: {{ $value }}
    {{- end }}
  template:
    metadata:
      labels:
        {{- include "hcloud-cloud-controller-manager.labels" . | nindent 8 }}
