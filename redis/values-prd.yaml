image:
  registry: docker.io
  repository: bitnami/redis
  tag: 7.0.15-debian-12-r0
  pullPolicy: IfNotPresent
redis:
  architecture: standalone
  persistence:
    enabled: false
  auth:
    enabled: false
