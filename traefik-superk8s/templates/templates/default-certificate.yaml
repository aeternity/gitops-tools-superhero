{{- if and (.Capabilities.APIVersions.Has "cert-manager.io/v1") .Values.defaultCertificateDmains -}}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-default-hetzner-cert
  labels:
    solver: hetzner
spec:
  secretName: traefik-default-hetzner-cert
  dnsNames:
    {{- toYaml .Values.defaultCertificateDmains | nindent 4 }}
  issuerRef:
    name: letsencrypt-hetzner
    # We can reference ClusterIssuers by changing the kind here.
    # The default value is Issuer (i.e. a locally namespaced Issuer)
    kind: ClusterIssuer
    group: cert-manager.io
{{- end -}}
