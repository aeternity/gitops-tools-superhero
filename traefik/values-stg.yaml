defaultCertificateDmains:
  - "*.stg.service.aepps.com"

traefik:
  instanceLabelOverride: traefik
  globalArguments: []
  additionalArguments:
    - "--entrypoints.web.http.redirections.entrypoint.to=:443"
    - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
  logs:
    general:
      level: DEBUG
    access:
      enabled: true
  serviceAccount:
    enabled: true  # Replaced "create" with "enabled"
    name: traefik-sa # Optional: you can define a specific name for the service account
  ports:
    websecure:
      tls:
        enabled: true
  tlsStore:
    default:
      defaultCertificate:
        secretName: traefik-default-cert
  service:
    enabled: true
    type: LoadBalancer
    annotations:
      load-balancer.hetzner.cloud/name: "traefik-lb"
      load-balancer.hetzner.cloud/location: "nbg1"
      load-balancer.hetzner.cloud/type: "lb11"
      load-balancer.hetzner.cloud/use-private-ip: "true"
      load-balancer.hetzner.cloud/ipv6-disabled: "true"
      load-balancer.hetzner.cloud/disable-private-ingress: "true"
      load-balancer.hetzner.cloud/protocol: "tcp"
      load-balancer.hetzner.cloud/hostname: "stg.service.aepps.com"
